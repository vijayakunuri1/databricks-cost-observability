[project]
name = "databricks-cost-observability"
version = "1.0.0"
description = "DBU and cost monitoring dashboard via Databricks system tables"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "databricks-sdk>=0.40.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
]
